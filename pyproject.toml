[project]
name = "light-for-videocall"
version = "0.1.0"
description = "Edge light overlay for video calls"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pygobject>=3.42.0",
    "pycairo>=1.20.0",
]
